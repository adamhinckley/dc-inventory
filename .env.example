# Compose placeholders only. Copy to `.env` at the repo root if you override defaults.
# Never commit a real `.env` (OP8). These values are dummy demo credentials.
# API process env lives in `apps/api/.env.example` (`DATABASE_URL`, `PORT`).

POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=dc_inventory
POSTGRES_PORT=5432

MINIO_ROOT_USER=minio
MINIO_ROOT_PASSWORD=minio-placeholder
MINIO_API_PORT=9000
MINIO_CONSOLE_PORT=9001

# Phase 1 local login placeholders (API process). Never commit a real secret or hash.
PHASE1_STAFF_PASSWORD=phase1-staff-placeholder
PHASE1_WHOLESALE_PASSWORD=phase1-wholesale-placeholder
