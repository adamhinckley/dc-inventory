# Frozen folder map; agent-optimized Done criteria

Scaffold commits the architecture folder map and forbids grab-bag utility packages. Empty bounded-context packages are not pre-created. Done criteria include root `AGENTS.md`, verbatim `pnpm` scripts, work-packet-shaped Linear issues, per-package one-screen READMEs where packages exist, and golden-path examples (Ping use case + in-memory test; Internal `DataTable` page; Wholesale product-card list) so later agents copy structure instead of inventing it.
