{
  "name": "@dc-inventory/inventory",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Inventory context: stock ledger, movements, and snapshot read model.",
  "exports": {
    ".": "./src/index.ts",
    "./schema": "./src/persistence/schema.ts",
    "./ledger-rules": "./src/domain/ledger-rules.ts",
    "./snapshot": "./src/domain/snapshot.ts",
    "./test-support/evaluate-demand-projection-sql": "./tests/support/evaluate-demand-projection-sql.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@dc-inventory/shared-kernel": "workspace:*",
    "drizzle-orm": "^0.45.2"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.8",
    "@types/node": "^26.2.0",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  },
  "engines": {
    "node": ">=24"
  }
}
