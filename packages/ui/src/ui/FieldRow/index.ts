/**
 * Horizontal labeled-field row. Controls share a baseline via `items-end`.
 *
 * @when A row of Label+Input/Select/Combobox with a trailing Button.
 * @avoid Aligning on labels (`items-center`) or putting `size="sm"` on the row action.
 */
export { FieldRow, LabeledField, type FieldRowProps, type LabeledFieldProps } from './FieldRow'
