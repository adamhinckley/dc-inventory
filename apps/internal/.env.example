# Leave empty to call `/internal/*` on this origin (rewritten to apps/api).
NEXT_PUBLIC_API_URL=

# Fastify origin used by next.config.ts rewrites.
# Local: http://localhost:3001
# Vercel (Production + Preview): https://dc-inventory-api.fly.dev
API_PROXY_ORIGIN=http://localhost:3001

# Optional local hostnames (pnpm dev:internal:local) — see scripts/local-dev-hosts.txt.
# Dashboard URL: http://dc-internal.test:3000
