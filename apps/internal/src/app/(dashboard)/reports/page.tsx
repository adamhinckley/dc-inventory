import { DashboardPlaceholder } from "../../../components/dashboard-placeholder";

export default function ReportsPage() {
  return (
    <DashboardPlaceholder
      title="Reports"
      body="Placeholder reports. Charts will consume x-chart report hooks — this page will not aggregate list rows."
    />
  );
}
