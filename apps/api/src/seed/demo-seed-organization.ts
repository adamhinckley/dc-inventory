import { OrganizationId } from "@dc-inventory/shared-kernel";

/** Demo book is always seeded on the DEFAULT organization — never a second company. */
export const DEMO_SEED_ORGANIZATION_ID = OrganizationId.DEFAULT;
