/** Demo customer whose Accounting tab is enriched after standard payment replay. */
export const CUSTOMER_ACCOUNTING_SHOWCASE_KEY = "idlePark" as const;

/** Minimum posted invoices required on the showcase customer before replay. */
export const CUSTOMER_ACCOUNTING_SHOWCASE_MIN_INVOICES = 4;
